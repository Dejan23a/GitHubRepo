pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Run Python test') {
            steps {
                bat 'py --version'
                bat 'py jenkins_test1.py'
            }
        }
    }
}