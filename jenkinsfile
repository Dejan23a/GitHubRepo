pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Run Python test') {
            steps {
                bat 'py --version'
                bat 'python jenkins_test1.py'
            }
        }
    }
}